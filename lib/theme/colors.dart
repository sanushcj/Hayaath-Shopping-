import 'package:flutter/material.dart';

class Pallete {
  // COLORS
  static const appBarGradient = LinearGradient(
    colors: [
      Colors.purple,
      Color(0xFFD1C4E9),
    ],
    stops: [0.5, 1.0],
  );

  static const secondaryColor = Color.fromRGBO(0, 0, 0, 1);
  static const backgroundColor = Color.fromRGBO(209, 196, 233, 1);
  static const Color greyBackgroundCOlor = Color.fromRGBO(206, 147, 216, 1);
  static const selectedNavBarColor = Color.fromARGB(255, 106, 27, 154);
  static const unselectedNavBarColor = Colors.black87;
  // STATIC IMAGES
}
